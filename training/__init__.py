"""DriftWatch training: baseline, tuned XGBoost, and registry promotion, all tracked in Azure ML via MLflow.

Run from the repo root as modules (``python -m training.train ...``) with the variables from
``.env.example`` exported.
"""
