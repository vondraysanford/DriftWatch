# Claude Code local context (convention from DocQuery/AgentReview: not public)
CLAUDE.md
.claude/settings.local.json

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Local config & OS noise
.env
.DS_Store
mlruns/

# Raw data lives under DVC, never in git
data/raw/

# Model fetched from the registry at build time (python -m serving.fetch_model)
serving/model/
