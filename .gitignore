# Claude Code local context (convention from DocQuery/AgentReview: not public)
CLAUDE.md
.claude/settings.local.json

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Local config & OS noise
.env
.DS_Store
mlruns/
mlflow.db

# Raw data lives under DVC, never in git
data/raw/

# Model fetched from the registry at build time (python -m serving.fetch_model)
serving/model/

# Scoring-code snapshot assembled right before a managed-endpoint deployment
infra/managed-endpoint/code/

# Drift reports, replay results, and verdicts (run artifacts; CI uploads them)
monitoring/out/

# Dashboard: dependencies and the build output (built before each image build, like the model)
dashboard/node_modules/
dashboard/dist/

# Downloaded GitHub Actions logs and artifacts used while diagnosing runs
Failed_deploy_prove_teardown_logs/
managed-endpoint-evidence/
