# Pinned per CLAUDE.md working agreements; versions verified against PyPI on 2026-08-23.
# Python 3.11 notes: numpy/xgboost are the last 3.11-compatible releases (newer need >=3.12),
# mlflow is capped at 3.13.0 by azureml-mlflow, pandas is capped below 3.0 by mlflow.

# Data & modeling
numpy==2.4.6
pandas==2.3.3
pyarrow==24.0.0  # parquet I/O for pipeline outputs; pinned explicitly rather than relied on via mlflow
scikit-learn==1.9.0
xgboost==3.2.0
optuna==4.9.0

# Tracking & monitoring
mlflow==3.13.0
azureml-mlflow==1.62.0.post5
evidently==0.7.21
dvc[azure]==3.67.1

# Serving
fastapi==0.141.1
uvicorn==0.52.4

# Azure SDK
azure-ai-ml==1.34.1
