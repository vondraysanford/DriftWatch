"""DriftWatch serving: FastAPI ``/predict`` on raw cycle windows, model baked in at build time,
every prediction logged to a configuration-driven sink (Postgres locally, Azure Blob on Container Apps).
"""
