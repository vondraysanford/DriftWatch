# Serving image only. Pinned to the same versions as the root requirements.txt where they overlap,
# so the unpickled model meets the library versions it was trained with.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
numpy==2.4.6
pandas==2.3.3
scikit-learn==1.9.0
mlflow-skinny==3.13.0
azure-storage-blob==12.27.1
azure-identity==1.25.3
# Postgres driver for the local prediction log (unused by the blob sink on Container Apps)
psycopg[binary]==3.3.5
