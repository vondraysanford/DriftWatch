# Development-only tooling (notebook kernel for exploration). Never installed into
# the serving image. Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Exploration
ipykernel==7.3.0
